export interface D1DiagnosticRow { strategicSwitch: boolean; forcedSwitch: boolean; suppressed: boolean; returnSwitch: boolean; diagnosticsError?: boolean; debugPlanScoreBreakdown?: unknown; }
export interface D1DiagnosticAggregate { strategicSwitches: number; strategicDecisionCount: number; strategicSwitchRate: number; forcedSwitches: number; forcedDecisionCount: number; forcedSwitchRate: number; suppressions: number; suppressionDecisionCount: number; switchSuppressionRate: number; returnSwitches: number; returnDecisionCount: number; aToBToARate: number; diagnosticsErrorCount: number; debugIncluded: false; }
export function aggregateD1Diagnostics(rows: D1DiagnosticRow[]): D1DiagnosticAggregate { const strategicSwitches = rows.filter((row) => row.strategicSwitch).length; const forcedSwitches = rows.filter((row) => row.forcedSwitch).length; const suppressions = rows.filter((row) => row.suppressed).length; const returnSwitches = rows.filter((row) => row.returnSwitch).length; return { strategicSwitches, strategicDecisionCount: rows.length, strategicSwitchRate: rows.length === 0 ? 0 : strategicSwitches / rows.length, forcedSwitches, forcedDecisionCount: rows.length, forcedSwitchRate: rows.length === 0 ? 0 : forcedSwitches / rows.length, suppressions, suppressionDecisionCount: rows.length, switchSuppressionRate: rows.length === 0 ? 0 : suppressions / rows.length, returnSwitches, returnDecisionCount: rows.length, aToBToARate: rows.length === 0 ? 0 : returnSwitches / rows.length, diagnosticsErrorCount: rows.filter((row) => row.diagnosticsError).length, debugIncluded: false }; }
export function assertD1DiagnosticsPrivacy(value: unknown): true { if (containsPrivate(value)) throw new Error("D1_DIAGNOSTICS_PRIVACY_VIOLATION"); return true; }
function containsPrivate(value: unknown): boolean { if (Array.isArray(value)) return value.some(containsPrivate); if (value === null || typeof value !== "object") return false; return Object.entries(value).some(([key, child]) => /^(partnerHand|opponentsHands|hands|deck|hiddenInitialHand|fullState|planScoreBreakdown|decisionSequence)$/i.test(key) || containsPrivate(child)); }
